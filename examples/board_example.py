"""Demo board (some global config)
"""


def _():
    """Optional slide title
    Markdown syntax
    - Bullet list
    - Bullet list

    1. Numbered list
    1. Numbered list

    > Quote of some kind
    > _~Some Known Person~_

    _Python code:_
    """

    def fn(x):
        return x**2

    x = 2


def _():
    """lol
    Given the following function, what is the result of `fn(2)`?
    """

    def fn(x):
        return x**2


def _():
    """
    Given the following function, what is the result of `fn(2)`?
    """
    x = 1234
    yield
    x = 1236
    yield
    x = 1236


def _(hide_code):
    """lol fdkjdsfkjfs
    Y0l0
    fdsfdsfdsf
    fddsfdsf
    """
    from math import pi as PI

    def fn(x):
        return x / PI


def lol_(raw):
    """Print text as is

    ┌────────────────────┐                 ┌────────────────┐
    │                    │                 │                │
    │ Important module   │◄─── message ───►│ Another module │
    │                    │                 │                │
    └────────────────────┘                 └────────────────┘
    """


pass
